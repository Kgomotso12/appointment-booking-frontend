services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: /api
    ports:
      - "9000:80"
      - "9443:443"
    environment:
      - NODE_ENV=production
    networks:
      - appointment-net

networks:
  appointment-net:
    external: true
